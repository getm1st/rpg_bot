def check_status (data):
    # если сбит с ног
    if data['status'] == "nock_down":
        # атака с помехой
        # атака по существу с преимуществом если нападающий в 5 футах от него, иначе с помехой
        None